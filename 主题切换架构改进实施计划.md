# 主题切换架构改进实施计划

## 一、实施目标

将主题切换从PyQt信号槽机制迁移到容器+EventBus模式，实现：
1. 架构统一：主题系统与系统框架（ServiceContainer + EventBus）完全一致
2. 降低耦合：组件通过EventBus解耦，无需直接引用theme_manager
3. 提升可维护性：生命周期管理统一，代码更清晰
4. 增强扩展性：支持多实例、易于扩展新功能
5. 改善可测试性：单元测试更容易编写和维护

## 二、实施阶段

### 阶段1：基础架构（1-2天）

#### 任务1.1：修改ThemeManager支持EventBus
**文件**：`utils/theme.py`

**修改内容**：
```python
from core.events.event_bus import get_event_bus
from core.events.events import ThemeChangedEvent

class ThemeManager(QObject):
    def __init__(self, config_manager: Optional[ConfigManager] = None):
        super().__init__()
        # 保留原有的初始化逻辑
        self._config_manager = config_manager or ConfigManager()
        self._event_bus = get_event_bus()
        
        # 初始化数据库
        self._init_database()
        
        # 扫描主题
        self._scan_qss_themes()
        self._scan_json_themes()
        
        # 加载保存的主题
        self._load_saved_theme()
    
    def set_theme(self, theme_name: str):
        # ... 切换主题逻辑
        
        # 发布主题变化事件（替代信号）
        event = ThemeChangedEvent(
            theme_name=theme_name,
            theme_config=self.get_theme_colors()
        )
        self._event_bus.publish(event)
```

**验收标准**：
- [ ] ThemeManager成功初始化EventBus
- [ ] 保留原有的数据库、主题扫描逻辑
- [ ] set_theme方法发布ThemeChangedEvent
- [ ] 单元测试通过

#### 任务1.2：配置EventBus（可选）
**文件**：`main.py`

**修改内容**：
```python
from core.events.event_bus import EventBus

# 创建带去重的EventBus（避免重复事件）
event_bus = EventBus(deduplication_window=0.5)  # 0.5秒去重窗口
set_event_bus(event_bus)
```

**验收标准**：
- [ ] EventBus配置完成
- [ ] 去重窗口设置为0.5秒
- [ ] 全局EventBus实例设置成功

#### 任务1.3：编写单元测试
**文件**：`tests/test_theme_manager.py`（新建）

**测试内容**：
```python
import pytest
from core.events.event_bus import EventBus, set_event_bus
from core.events.events import ThemeChangedEvent
from utils.theme import ThemeManager

def test_theme_manager_publishes_event():
    """测试ThemeManager发布主题变化事件"""
    # 创建测试EventBus
    test_bus = EventBus()
    set_event_bus(test_bus)
    
    # 创建ThemeManager
    theme_manager = ThemeManager()
    
    # 订阅事件
    events_received = []
    def on_theme_changed(event):
        events_received.append(event)
    
    test_bus.subscribe(ThemeChangedEvent, on_theme_changed)
    
    # 设置主题
    theme_manager.set_theme('light')
    
    # 验证事件发布
    assert len(events_received) == 1
    assert events_received[0].theme_name == 'light'

def test_event_bus_deduplication():
    """测试EventBus事件去重"""
    test_bus = EventBus(deduplication_window=0.5)
    set_event_bus(test_bus)
    
    # 快速发布相同事件
    for _ in range(10):
        test_bus.publish(ThemeChangedEvent(theme_name='light', theme_config={}))
    
    # 验证去重效果
    # 由于去重窗口0.5秒，大部分事件应该被去重
```

**验收标准**：
- [ ] 单元测试文件创建
- [ ] 测试覆盖率>80%
- [ ] 所有测试通过

---

### 阶段2：核心组件迁移（2-3天）

#### 任务2.1：迁移EnhancedStrategyManagerDialog
**文件**：`gui/dialogs/enhanced_strategy_manager_dialog.py`

**修改内容**：
```python
from core.events.event_bus import get_event_bus
from core.events.events import ThemeChangedEvent

class EnhancedStrategyManagerDialog(QDialog):
    def __init__(self, parent=None, theme_manager=None):
        super().__init__(parent)
        
        # 通过依赖注入获取主题管理器
        from utils.theme import get_theme_manager
        self.theme_manager = theme_manager or get_theme_manager()
        
        # 订阅主题变化事件
        self._event_bus = get_event_bus()
        self._event_bus.subscribe(
            ThemeChangedEvent,
            self._on_theme_changed
        )
        
        # 应用初始主题
        self.theme_manager.apply_theme(self)
    
    def _on_theme_changed(self, event: ThemeChangedEvent):
        """主题变化事件处理"""
        # 根据主题类型处理
        if hasattr(event, 'theme_type') and event.theme_type == 'qss':
            # QSS主题：全局应用，不需要对每个widget调用apply_theme
            pass
        else:
            # JSON主题：对当前widget应用主题
            self.theme_manager.apply_theme(self)
    
    def closeEvent(self, event):
        """清理事件订阅"""
        self._event_bus.unsubscribe(
            ThemeChangedEvent,
            self._on_theme_changed
        )
        
        # 清理PyQt信号连接（如果保留）
        if hasattr(self, '_theme_connection'):
            self.theme_manager.theme_changed.disconnect(self._on_theme_changed)
        
        super().closeEvent(event)
```

**验收标准**：
- [ ] EnhancedStrategyManagerDialog通过EventBus订阅主题变化
- [ ] 主题切换功能正常工作
- [ ] 对话框关闭时清理事件订阅
- [ ] QSS主题和JSON主题都正确处理

#### 任务2.2：迁移MainWindow
**文件**：`gui/main_window.py`

**修改内容**：与EnhancedStrategyManagerDialog类似

**验收标准**：
- [ ] MainWindow通过EventBus订阅主题变化
- [ ] 主题切换功能正常工作
- [ ] 窗口关闭时清理事件订阅

#### 任务2.3：迁移其他核心对话框
**文件**：
- `gui/dialogs/strategy_config_dialog.py`
- `gui/dialogs/trading_dialog.py`
- 其他核心对话框

**验收标准**：
- [ ] 所有核心对话框通过EventBus订阅主题变化
- [ ] 主题切换功能正常工作
- [ ] 对话框关闭时清理事件订阅

#### 任务2.4：验证主题切换功能
**测试内容**：
1. 切换QSS主题，验证所有UI更新
2. 切换JSON主题，验证所有UI更新
3. 快速切换主题，验证去重功能
4. 关闭对话框后切换主题，验证无错误

**验收标准**：
- [ ] QSS主题切换正常
- [ ] JSON主题切换正常
- [ ] 事件去重功能正常
- [ ] 无内存泄漏

---

### 阶段3：全面迁移（3-5天）

#### 任务3.1：迁移所有子组件
**文件**：所有使用theme_manager的组件

**组件列表**：
- `gui/dialogs/*`（所有对话框）
- `gui/widgets/*`（所有自定义控件）
- `gui/panels/*`（所有面板）

**验收标准**：
- [ ] 所有组件通过EventBus订阅主题变化
- [ ] 所有组件清理事件订阅

#### 任务3.2：清理不再需要的信号连接代码
**文件**：所有迁移的组件

**清理内容**：
- 删除 `self.theme_manager.theme_changed.connect(...)` 代码
- 删除 `self.theme_manager.theme_changed.disconnect(...)` 代码
- 删除 `_theme_connection_connected` 相关代码

**验收标准**：
- [ ] 所有PyQt信号连接代码已删除
- [ ] 代码审查通过

#### 任务3.3：更新文档和注释
**文件**：
- `docs/theme_architecture.md`（新建）
- 代码注释

**文档内容**：
```markdown
# 主题系统架构

## 架构概述

主题系统使用**服务容器+事件总线**模式：

- **ThemeManager**：作为Singleton服务注册到ServiceContainer
- **EventBus**：负责主题变化事件的发布和订阅
- **组件**：通过EventBus订阅ThemeChangedEvent

## 使用方式

### 订阅主题变化
```python
from core.events.event_bus import get_event_bus
from core.events.events import ThemeChangedEvent

class MyWidget(QWidget):
    def __init__(self):
        super().__init__()
        
        # 订阅主题变化事件
        self._event_bus = get_event_bus()
        self._event_bus.subscribe(
            ThemeChangedEvent,
            self._on_theme_changed
        )
    
    def _on_theme_changed(self, event: ThemeChangedEvent):
        """处理主题变化"""
        # 根据主题类型处理
        if hasattr(event, 'theme_type') and event.theme_type == 'qss':
            # QSS主题：全局应用
            pass
        else:
            # JSON主题：应用主题
            from utils.theme import get_theme_manager
            theme_manager = get_theme_manager()
            theme_manager.apply_theme(self)
    
    def closeEvent(self, event):
        """清理事件订阅"""
        self._event_bus.unsubscribe(
            ThemeChangedEvent,
            self._on_theme_changed
        )
        super().closeEvent(event)
```

### 发布主题变化

```python
from core.events.events import ThemeChangedEvent
from core.events.event_bus import get_event_bus

# 发布主题变化事件
event_bus = get_event_bus()
event_bus.publish(
    ThemeChangedEvent(
        theme_name='light',
        theme_config={'background': '#FFFFFF', 'text': '#222b45'}
    )
)
```

## 主题类型

### QSS主题
- 通过 `app.setStyleSheet()` 全局应用
- 不需要对每个widget调用 `apply_theme()`
- 性能更好，一次全局应用

### JSON主题
- 需要对每个widget调用 `apply_theme()`
- 遍历所有窗口和子组件
- 性能相对较差，需要异步刷新
```

**验收标准**：
- [ ] 文档创建完成
- [ ] 代码注释更新完成
- [ ] 文档审查通过

#### 任务3.4：性能测试和优化
**测试内容**：
1. 测试主题切换响应时间
2. 测试大量事件处理性能
3. 测试内存使用情况
4. 测试CPU使用情况

**验收标准**：
- [ ] 主题切换响应时间<100ms
- [ ] 性能无明显下降（<5%）
- [ ] 无内存泄漏
- [ ] 无CPU峰值

---

### 阶段4：清理和优化（1-2天）

#### 任务4.1：移除PyQt信号
**文件**：`utils/theme.py`

**修改内容**：
```python
class ThemeManager(QObject):
    # 移除PyQt信号（如果确认不再需要）
    # theme_changed = pyqtSignal(Theme)  # 删除此行
    
    def set_theme(self, theme_name: str):
        # ... 切换主题逻辑
        
        # 只通过EventBus发布事件
        event = ThemeChangedEvent(
            theme_name=theme_name,
            theme_config=self.get_theme_colors()
        )
        self._event_bus.publish(event)
        # self.theme_changed.emit(self._current_theme)  # 删除此行
```

**验收标准**：
- [ ] PyQt信号定义已删除
- [ ] 只通过EventBus发布事件
- [ ] 代码审查通过

#### 任务4.2：优化EventBus性能
**文件**：`main.py`

**优化内容**：
```python
from core.events.event_bus import EventBus

# 根据实际需求配置EventBus
event_bus = EventBus(
    async_execution=False,  # 同步执行，性能更好
    deduplication_window=0.5,  # 0.5秒去重窗口
    max_workers=4  # 如果使用异步，最大4个工作线程
)
set_event_bus(event_bus)
```

**验收标准**：
- [ ] EventBus配置优化完成
- [ ] 性能测试通过
- [ ] 去重功能正常

#### 任务4.3：添加监控和日志
**文件**：`utils/theme.py`

**添加内容**：
```python
from loguru import logger

class ThemeManager(QObject):
    def set_theme(self, theme_name: str):
        # ... 切换主题逻辑
        
        # 添加性能监控
        import time
        start_time = time.time()
        
        # 发布主题变化事件
        event = ThemeChangedEvent(
            theme_name=theme_name,
            theme_config=self.get_theme_colors()
        )
        self._event_bus.publish(event)
        
        # 记录性能指标
        elapsed_time = time.time() - start_time
        logger.info(
            f"主题切换完成: {theme_name}, "
            f"耗时: {elapsed_time*1000:.2f}ms, "
            f"事件类型: {event.theme_type if hasattr(event, 'theme_type') else 'unknown'}"
        )
        
        # 获取EventBus性能统计
        stats = self._event_bus.get_stats()
        logger.debug(f"EventBus统计: {stats}")
```

**验收标准**：
- [ ] 性能监控日志添加完成
- [ ] EventBus统计日志添加完成
- [ ] 日志审查通过

#### 任务4.4：代码审查和重构
**审查内容**：
1. 检查所有主题相关代码
2. 检查所有事件订阅和清理
3. 检查所有导入和依赖
4. 检查性能和日志

**验收标准**：
- [ ] 代码审查完成
- [ ] 所有问题已修复
- [ ] 代码质量达标

---

## 三、风险管理

### 风险1：EventBus性能开销
**影响**：主题切换可能变慢
**概率**：低
**缓解措施**：
1. 使用同步执行模式（默认）
2. 事件去重（0.5秒窗口）
3. 性能监控（get_stats）
4. 性能测试和优化

### 风险2：迁移复杂度
**影响**：需要修改大量代码
**概率**：中
**缓解措施**：
1. 渐进式迁移（分4个阶段）
2. 保留向后兼容（可选保留PyQt信号）
3. 充分测试（每个阶段独立测试）
4. 代码审查（确保质量）

### 风险3：向后兼容性
**影响**：旧代码可能不工作
**概率**：低
**缓解措施**：
1. 保留PyQt信号作为备用（阶段1-3）
2. 文档说明（更新架构文档）
3. 版本兼容检查（添加版本检查）
4. 充分测试（测试所有场景）

### 风险4：调试难度
**影响**：EventBus调试不如信号直观
**概率**：低
**缓解措施**：
1. 添加详细日志（记录所有事件）
2. 事件追踪工具（get_stats）
3. 调试模式（添加调试开关）
4. 单元测试（覆盖所有场景）

---

## 四、成功标准

迁移成功的标准：

1. ✅ 所有组件通过EventBus订阅主题变化
2. ✅ ThemeManager注册为Singleton服务
3. ✅ 主题切换功能正常工作
4. ✅ 性能无明显下降（<5%）
5. ✅ 单元测试覆盖率>80%
6. ✅ 向后兼容性保持
7. ✅ 无内存泄漏
8. ✅ 无CPU峰值
9. ✅ 代码审查通过
10. ✅ 文档更新完成

---

## 五、时间表

| 阶段 | 任务 | 预计时间 | 负责人 | 状态 |
|--------|------|-----------|--------|------|
| 阶段1 | 基础架构 | 1-2天 | 待定 | 待开始 |
| 阶段2 | 核心组件迁移 | 2-3天 | 待定 | 待开始 |
| 阶段3 | 全面迁移 | 3-5天 | 待定 | 待开始 |
| 阶段4 | 清理和优化 | 1-2天 | 待定 | 待开始 |
| **总计** | | **7-12天** | | |

---

## 六、下一步行动

1. **评审方案**：与团队讨论此实施计划的可行性
2. **分配任务**：确定每个阶段的负责人和时间表
3. **技术验证**：创建原型验证EventBus性能
4. **开始实施**：按照渐进式迁移策略执行
5. **持续监控**：在迁移过程中监控性能和稳定性
6. **文档更新**：及时更新实施进度和遇到的问题
7. **代码审查**：每个阶段完成后进行代码审查
8. **性能测试**：每个阶段完成后进行性能测试
9. **用户验收**：迁移完成后进行用户验收测试
10. **部署上线**：测试通过后部署到生产环境

---

## 七、附录

### A. 相关文档
- [主题切换架构改进方案-容器Event模式.md](file:///d:/DevelopTool/FreeCode/HIkyuu-UI/hikyuu-ui/主题切换架构改进方案-容器Event模式.md)
- [策略管理器UI主题同步修复方案.md](file:///d:/DevelopTool/FreeCode/HIkyuu-UI/hikyuu-ui/策略管理器UI主题同步修复方案.md)

### B. 相关代码文件
- `utils/theme.py` - 主题管理器
- `core/events/events.py` - 事件定义
- `core/events/event_bus.py` - 事件总线
- `core/containers/service_container.py` - 服务容器
- `core/containers/service_registry.py` - 服务注册表

### C. 测试文件
- `tests/test_theme_manager.py` - 主题管理器测试（新建）
- `tests/test_event_bus.py` - 事件总线测试（新建）
- `tests/test_theme_integration.py` - 主题集成测试（新建）

### D. 性能指标
- 主题切换响应时间：<100ms
- 性能下降：<5%
- 内存使用：无明显增长
- CPU使用：无明显峰值
- 事件处理延迟：<10ms

### E. 代码质量指标
- 单元测试覆盖率：>80%
- 代码审查通过率：100%
- 文档完整性：100%
- 向后兼容性：100%
