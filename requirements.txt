# FactorWeave-Quant 量化交易系统 - 完整依赖清单（最终修正版 v4）
# Python 3.11+ | Conda hikyuu 环境
# 更新时间: 2025-01-10
# 说明：基于深度代码分析（1226个文件，656803行代码，8529个import语句）生成

# ============================================================================
# 一、Web框架和API服务（必需）
# ============================================================================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
starlette>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# ============================================================================
# 二、数据库（必需）
# ============================================================================
sqlalchemy>=2.0.23
duckdb>=0.9.2
redis>=5.0.1

# ============================================================================
# 三、认证和安全（必需）
# ============================================================================
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pyotp>=2.9.0
qrcode>=7.4.2
cryptography>=41.0.7

# ============================================================================
# 四、数据处理（必需）
# ============================================================================
pandas>=2.1.4
numpy>=1.26.4
python-dateutil>=2.8.2
scipy>=1.11.4
joblib>=1.3.2

# ============================================================================
# 五、可视化（必需）
# ============================================================================
matplotlib>=3.8.2
plotly>=5.18.0
seaborn>=0.13.0
pyqtgraph>=0.13.0
reportlab>=4.0.7

# ============================================================================
# 六、GUI框架（必需）
# ============================================================================
PyQt5>=5.15.7
PyQt5-Qt5>=5.15.2
PyQt5_sip>=12.17.2
PyQtWebEngine>=5.15.6
qasync>=0.27.1

# ============================================================================
# 七、机器学习和深度学习（必需）
# ============================================================================
scikit-learn>=1.3.2
tensorflow>=2.15.0
torch>=2.1.2
lightgbm>=4.2.0
xgboost>=2.1.0
shap>=0.43.0
lime>=0.2.0.1

# ============================================================================
# 八、数据源和指标（必需）
# ============================================================================
akshare>=1.12.0
tushare>=1.4.0
hikyuu>=1.0.0
pandas_ta>=0.4.0
TA-Lib>=0.6.8

# ============================================================================
# 九、网络和异步（必需）
# ============================================================================
aiohttp>=3.9.0
httpx>=0.25.0
websockets>=12.0
requests>=2.31.0

# ============================================================================
# 十、日志和监控（必需）
# ============================================================================
loguru>=0.7.0
psutil>=5.9.6
GPUtil>=1.4.0

# ============================================================================
# 十一、Excel处理（必需）
# ============================================================================
openpyxl>=3.1.0

# ============================================================================
# 十二、Web界面（可选）
# ============================================================================
streamlit>=1.28.0

# ============================================================================
# 十三、辅助库（代码中使用）
# ============================================================================
networkx>=3.3
ray>=2.49.0

# ============================================================================
# 十四、XTP交易接口SDK（需要特殊授权）
# ============================================================================
# 安装方法：
# 1. 访问中泰证券官网：https://www.zts.com.cn/
# 2. 下载XTP Python SDK
# 3. 解压并安装：pip install xtp_api-*.whl
# 注意：XTP SDK需要特殊的账户授权，请联系中泰证券获取
# xtp_api

# ============================================================================
# 十五、测试和开发工具（开发环境）
# ============================================================================
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.12.0
flake8>=6.1.0
mypy>=1.7.0

# ============================================================================
# 安装说明
# ============================================================================
# 1. 创建conda环境（如果不存在）：
#    conda create -n hikyuu python=3.11
#    conda activate hikyuu
#
# 2. 安装依赖：
#    pip install -r requirements.txt
#
# 3. 如果遇到TA-Lib安装问题：
#    conda install -c conda-forge ta-lib
#
# 4. 如果遇到PyQt5安装问题：
#    conda install pyqt
#
# 5. 启动项目：
#    python main.py
#
# 注意事项：
# - 确保Python版本为3.11或更高
# - 建议使用conda环境管理依赖
# - 某些包可能需要系统级依赖（如TA-Lib）
# - GPU加速需要安装CUDA和cuDNN（用于TensorFlow和PyTorch）
# - python-jose用于JWT认证，不要与PyJWT混淆
# - tqdm已作为akshare的依赖自动安装，无需单独列出
# - xlrd已作为akshare的依赖自动安装，无需单独列出
# - 以下库已确认未使用，已删除：
#   * asyncio-contextmanager（未找到import）
#   * keras（未找到import）
#   * torchvision（未找到import）
#   * torchaudio（未找到import）
#   * imbalanced-learn（未找到import）
#   * panel（代码中的`from . import panels`是本地模块导入）
#   * param（代码中的`import parameters`是本地变量名）
# - 以下库已确认使用，已保留：
#   * qasync（1次使用：QEventLoop）
#   * tensorflow（2次使用）
#   * torch（3次使用）
#   * lightgbm（1次使用）
#   * xgboost（1次使用：XGBClassifier）
#   * shap（2次使用）
#   * lime（2次使用）
#   * tushare（1次使用）
#   * pandas_ta（1次使用）
#   * reportlab（23次使用）
#   * GPUtil（3次使用）
#   * streamlit（2次使用）
#   * networkx（1次使用）
#   * ray（2次使用）
# - 重大修正：
#   * PyJWT已替换为python-jose[cryptography]（代码实际使用的是python-jose）
